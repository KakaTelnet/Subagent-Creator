interface:
  display_name: "Subagent Creator"
  short_description: "默认创建项目级、可选全局的多模型子 Agent"
  default_prompt: "Use $subagent-creator to design and configure the minimal sufficient project-scoped Codex subagent team by default; create global Agents only when I explicitly request them."
